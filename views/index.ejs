<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <title>QD Studio - Usługi Cyfrowe</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <header>
    <h1>QD Studio</h1>
    <p>Profesjonalne rozwiązania dla Discorda, Minecrafta i WWW — szybko, tanio, solidnie</p>
  </header>

  <main>
    <section class="grid">
      <% services.forEach(service => { %>
        <div class="card">
          <h2><%= service.title %></h2>
          <p class="price"><%= service.price %></p>
          <ul>
            <% service.features.forEach(feature => { %>
              <li><%= feature %></li>
            <% }) %>
          </ul>
          <button><%= service.button %></button>
        </div>
      <% }) %>
    </section>
  </main>

  <footer>
    <p>© 2025 QD Studio. Wszystkie prawa zastrzeżone.</p>
  </footer>
</body>
</html>
